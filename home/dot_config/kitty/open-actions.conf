# ── Open Actions ─────────────────────────────────────────────────
# Controls what happens when shift+clicking file paths in the terminal.
# Patterns are matched top-to-bottom; first match wins.
# See: https://sw.kovidgoyal.net/kitty/open_actions/

# Local files → VS Code
protocol file
action launch --type=background code ${FILE_PATH}
