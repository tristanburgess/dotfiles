{{- if eq .chezmoi.os "linux" -}}
#!/bin/bash
set -euo pipefail

if ! groups "$USER" | grep -qw docker; then
    sudo usermod -aG docker "$USER"
    printf "Added %s to docker group. Log out and back in for it to take effect.\n" "$USER"
fi
{{- end -}}
