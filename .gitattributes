# Force LF line endings everywhere — prevents CRLF phantom diffs
# in colocated jj+git repos on Windows (jj doesn't honor core.autocrlf).
* text=auto eol=lf

# Tell GitHub Linguist to highlight chezmoi templates as their target language.
*.sh.tmpl linguist-language=Shell
